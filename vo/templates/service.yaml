apiVersion: v1
kind: Service
metadata:
  name: {{ include "vo.fullname" . }}
  labels:
    {{- include "vo.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "vo.selectorLabels" . | nindent 4 }}
